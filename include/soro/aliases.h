#pragma once

namespace soro {

using meter_per_second = float;
using km_h = float;
using ton = float;

using train_physics_t = float;

using seconds = float;
using meters = float;

}  // namespace soro