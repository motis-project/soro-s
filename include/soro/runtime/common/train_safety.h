#pragma once

// #include "soro/runtime/common/pzb.h"

namespace soro::runtime {

struct train_safety {

  // si::speed get_max_speed(si::length const dist) const {
  //   pzb_.current_limit(dist);
  // }
  //
  // pzb pzb_;
};

}  // namespace soro::runtime
